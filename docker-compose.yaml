version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    volumes:
      - ./backend:/app/backend
    ports:
      - "8000:8000"
    environment:
      - ENV=development
      - PYTHONPATH=/app/backend

